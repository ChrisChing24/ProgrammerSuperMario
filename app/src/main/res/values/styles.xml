<resources>

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light" />

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular LiveApi-level can go here. -->
        <!--<item name="android:listDivider">@drawable/default_line</item>-->
    </style>

    <style name="BaseAppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--EditText提示文字颜色-->
        <item name="android:textColorHint">@color/text_gray</item>
        <!--TextInputEditText未操作时下划线和字体颜色-->
        <item name="colorControlNormal">@color/text_gray</item>
        <!--TextInputEditText操作时下划线和字体颜色-->
        <item name="colorControlActivated">@color/app_main_color</item>
        <item name="colorControlHighlight">@color/app_main_color</item>

    </style>

    <!--闪屏页主题-->
    <style name="SplashScreenTheme" parent="AppTheme">
        <item name="android:windowBackground">@mipmap/marvel_studios_logo</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- 向右箭头 -->
    <style name="NextIconStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:src">@mipmap/icon_right_arrow</item>
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:contentDescription">@string/image_contentDescription</item>
    </style>

    <!--蓝色大按钮   parent属性是去掉自带阴影-->
    <style name="BtnBlueStyle" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:background">@drawable/shape_corner_round_blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="CustomAlertDialogTheme" parent="AlertDialog.AppCompat.Light">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
        <item name="android:layout_gravity">center</item>
    </style>


</resources>
